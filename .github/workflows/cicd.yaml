name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo นี้ (มี manifests, cicd.yaml)
      - name: Checkout manifests repo
        uses: actions/checkout@v3

      # 2. Checkout อีก repo (มี Dockerfile)  
      - name: Checkout app repo
        uses: actions/checkout@v3
        with:
          repository: auttz/DockerFile_AppPython   # 👈 repo ที่มี Dockerfile
          path: app                               # clone ไว้ที่โฟลเดอร์ ./app

      # 3. Login Docker Hub
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # 4. Build Docker image (ใช้ Dockerfile จาก ./app)
      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/minikube:1.8 ./app

      # 5. Push Docker image
      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/minikube:1.8

  deploy:
    runs-on: self-hosted   # ✅ เครื่องที่มี Minikube
    needs: build           # ✅ จะรันหลัง build เสร็จ

    steps:
      # 1. Checkout manifests repo (เพื่อใช้ deployment.yaml, postgres.yaml)
      - name: Checkout manifests repo
        uses: actions/checkout@v3

      # 2. Start Minikube (ใช้ Docker driver)
      - name: Start Minikube
        shell: cmd
        run: |
          minikube start --driver=docker
          kubectl config use-context minikube

      # 3. Deploy ไป Minikube
      - name: Deploy to Minikube
        shell: cmd
        run: |
          kubectl apply -f deployment.yaml
          kubectl apply -f postgres.yaml
          kubectl rollout status deployment/myapp
